<h2 class="book-brand">
  <a class="flex align-center" href="{{ '' | absoluteUrl }}">
    {% if site.icon %}  
    <img src="{{ site.icon }}" alt="Logo" />
  {% endif %}
    <span>{{ site.title }}</span>
  </a>
</h2>
